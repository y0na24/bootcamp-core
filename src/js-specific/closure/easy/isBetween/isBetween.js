// Сделай функцию inBetween(a, b), которая возвращает функцию для фильтрации массива по диапазону.
function inBetween(a, b) {}

const arr = [1, 2, 3, 4, 5, 6];
console.log(arr.filter(inBetween(2, 4))); // [2, 3, 4]
